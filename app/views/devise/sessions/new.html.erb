<div class="intro">
    <h1>Please forgive our mess.  This project is undergoing a major face-lift</h1>
    <h2>Please feel free to poke around during our remodel</h2>
    <h3>All locations and discounts listed are supplied by your fellow members</h3>
    <h4>We here at "CODE FOUR DISCOUNTS" do not guarantee any information beyond this point</h4>
  <br />
</div>

<h2>Log in</h2>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
